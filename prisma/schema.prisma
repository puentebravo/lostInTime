generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model posts {
  ID      String  @id @unique(map: "ID") @default(uuid()) @db.Char(36)
  content String  @db.LongText
  tags    String? @db.VarChar(20)
}
